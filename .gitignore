/target
/.fleet
/_reference
*.log
_secrets/

# Private hand-off and design notes, never published. The one markdown that
# ships is the agent skill: the binary embeds it (`starcil --skill`).
*.md
!/skills/starcil/SKILL.md
!/README.md
/docs/
# Machine-local build wrapper (patched GNU toolchain paths) and probe output.
/build.ps1
/probe-mouse.txt
/tests/e2e/campaign-output.txt
/dist/
# VPS deploy helper (Xtarify infrastructure details), local only.
/deploy-site.sh
/_site/
/site-stats.sh
/linux-build.sh
